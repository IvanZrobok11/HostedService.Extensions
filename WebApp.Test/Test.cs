
namespace HostedService.Extensions;

/// <summary>
/// Абстрактний клас для створення фонових служб, які виконуються за розкладом
/// </summary>
//public class TestScheduledBackgroundService(IServiceProvider services, ILogger logger) : ScheduledBackgroundService(services, logger)
//{
//    protected override Every Scheduler => Every.Minutes(1);

//    protected override async Task ExecuteAsync(CancellationToken cancellationToken, IServiceScope scope)
//    {
//        await Console.Out.WriteLineAsync("Hello from Test");
//    }
//}
